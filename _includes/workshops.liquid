<div class="col">
  <a href="{% if workshop.redirect %}{{ workshop.redirect }}{% else %}{{ workshop.url | relative_url }}{% endif %}">
    <div class="card h-100 hoverable">
      {% if workshop.img %}
        {%
          include figure.liquid
          loading="eager"
          path=workshop.img
          sizes = "250px"
          alt="workshop thumbnail"
          class="card-img-top"
        %}
      {% endif %}
      <div class="card-body">
        <h2 class="card-title">{{ workshop.title }}</h2>
        <p class="card-text">{{ workshop.description }}</p>
        <div class="row ml-1 mr-1 p-0">
          {% if workshop.github %}
            <div class="github-icon">
              <div class="icon" data-toggle="tooltip" title="Code Repository">
                <a href="{{ workshop.github }}"><i class="fa-brands fa-github gh-icon"></i></a>
              </div>
              {% if workshop.github_stars %}
                <span class="stars" data-toggle="tooltip" title="GitHub Stars">
                  <i class="fa-solid fa-star"></i>
                  <span id="{{ workshop.github_stars }}-stars"></span>
                </span>
              {% endif %}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  </a>
</div>
